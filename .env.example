# Add a Gemini key to the local environment or .env.local for development.
# Set the same key as GEMINI_API_KEY in Vercel Preview and Production environments.
GEMINI_API_KEY=
# Optional: override the current default model.
# GEMINI_MODEL=gemini-2.5-flash
